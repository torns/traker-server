module.exports = app => {
    const checkLogin = app.middleware.checkLogin({});
    const prefix="/page";
  
    // app.router.post(prefix + '/findOrCreate', app.controller.page.findOrCreate);
    
  };
  